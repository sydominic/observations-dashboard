# v26 Letter Opinion2 Hotfix - Layout and Period Change Error

## 변경사항
- 2페이지 `반복 가능 관리취약점 + 보완방향` 양식을 더 명확히 반영했습니다.
  - 중점 점검항목 컬럼에서 `항목 1/2/3` 보조 라벨 제거
  - 상단 설명문 추가: 상위 반복 지적을 단순 자료목록이 아니라 관리취약점/보완방향 관점으로 정리
  - 표 높이와 글자 크기 조정
  - 문구를 의견2 예시 취지에 맞게 조정
- 1페이지 하단 표의 중점 점검항목 표시도 `항목 1/2/3` 대신 실제 항목명으로 표시되도록 변경했습니다.
- PDF 표 렌더링 시 빈 데이터로 인해 matplotlib table 오류가 발생하지 않도록 빈 행 방어로직을 추가했습니다.
- 기간 변경 시 이전 PDF 캐시가 재사용되지 않도록 Letter 캐시 목적값을 갱신했습니다.
- 기간 변경 후 PDF 생성 오류가 발생하더라도 전체 앱이 중단되지 않도록 Letter / 내부감시용 Checklist / One page Checklist 생성부에 예외 처리와 로그 기록을 추가했습니다.

## 검증
- `python -m py_compile streamlit_app.py` 통과
- 테스트 데이터 기준 `2026-1Q`, `2026-2Q`, `2026-연간` Letter PDF 생성 확인
- 생성 PDF에서 `Track`, `선택 기간 핵심`, `관리흐름`, `흐름에 영향`, `다음 기간 모니터링`, `본 페이지는`, `자체점검 질문` 문구 미검출 확인
